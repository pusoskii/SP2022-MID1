// Write a program that for given value of x (during the declaration) will compute and print the value of x^2.

#include <stdio.h>

int main() {
    int x = 25;
    printf("%d squared is equal to %d", x, x * x);

    return 0;
}