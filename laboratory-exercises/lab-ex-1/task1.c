#include <stdio.h>

int main() {
    int firstNumber, secondNumber;

    scanf("%d%d", &firstNumber, &secondNumber);
    printf("%d\n", firstNumber + secondNumber);
    printf("%d\n", firstNumber * secondNumber);
    return 0;
}